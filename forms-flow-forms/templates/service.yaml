apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.name }}
  name: {{ .Values.name }}
spec:
  ports:
  - name: "3001"
    port: 3001
    targetPort: 3001
  selector:
    app: {{ .Values.name }}
status:
  loadBalancer: {}
