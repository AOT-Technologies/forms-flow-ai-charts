apiVersion: v1
kind: PersistentVolume
metadata:
  name: redash-pv
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/redash-pv

