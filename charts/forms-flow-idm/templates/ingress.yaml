apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    cert-manager.io/cluster-issuer: {{ .Values.keycloak.sslcertificate.issuer }}
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "Content-Security-Policy: upgrade-insecure-requests";
      more_set_headers 'Strict-Transport-Security: max-age=63072000; includeSubDomains; preload';
spec:
  tls:
    - hosts:
      - {{ .Values.Domain }}
      secretName: {{ .Values.keycloak.sslcertificate.secretName }}
  rules:
    - host: {{ .Values.Domain }}
      http:
        paths:
          - path: /auth
            pathType: Prefix
            backend:
              service:
                 name: {{ .Chart.Name }}-keycl
                 port:
                    number: 80